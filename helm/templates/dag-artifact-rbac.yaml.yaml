{{- if .Values.dagArtifact.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Release.Name }}-dag-artifact

---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ .Release.Name }}-dag-artifact
rules:
  - apiGroups: [""]
    resources: ["configmaps"]
    verbs: ["get","create","update","patch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ .Release.Name }}-dag-artifact
subjects:
  - kind: ServiceAccount
    name: {{ .Release.Name }}-dag-artifact
roleRef:
  kind: Role
  name: {{ .Release.Name }}-dag-artifact
  apiGroup: rbac.authorization.k8s.io
{{- end }}
